# 09 — Sprint v1.2: Self Healing & Alert Multi-Channel

**Target:** Self-healing otomatis + alert via Telegram/Discord/Email
**Estimasi:** 2 minggu
**Dependensi:** v1.1 selesai

---

## Week 1: Alert Multi-Channel

### 1.1 Migration & Model
- [ ] Migration: `alert_channels`, `alert_notifications`
- [ ] Model: `AlertChannel`, `AlertNotification`
- [ ] `AlertChannelType` enum
- [ ] Update `alerts` table: tambah kolom `resolved_at`, `resolved_by`, `acknowledged_at`, `acknowledged_by`

### 1.2 Channel Implementation

```php
interface AlertChannelInterface
{
    public function send(Alert $alert): array; // return ['status' => 'sent'|'failed', 'response' => '...']
}
```

- [ ] `TelegramChannel` — kirim via Bot API `sendMessage`
- [ ] `DiscordChannel` — kirim via Webhook embed
- [ ] `EmailChannel` — kirim via Laravel Mail (configurable SMTP)

### 1.3 Alert Dispatcher
- [ ] `AlertDispatcher` — iterate all enabled channels, dispatch Alert per channel
- [ ] `DispatchAlertJob` — queue per channel
- [ ] `AlertNotificationController@index`, `@show`, `@retry`
- [ ] Retry logic: jika gagal, retry 3x dengan backoff

### 1.4 Alert Channel UI
- [ ] `AlertChannelController@index`, `@store`, `@show`, `@update`, `@destroy`, `@test`, `@toggle`
- [ ] `AlertChannelResource`
- [ ] UI form: channel type selector + config fields
- [ ] "Test" button — kirim test alert ke channel

### 1.5 Alert Management UI
- [ ] Alert list dengan filter severity, status, server, date
- [ ] Alert detail: message, context, related notifications, healing attempts
- [ ] Acknowledge / Resolve button
- [ ] Alert summary widget di dashboard: "3 critical, 5 warning, 0 info"

---

## Week 2: Self Healing

### 2.1 Migration & Model
- [ ] Migration: `healing_rules`, `healing_logs`
- [ ] Model: `HealingRule`, `HealingLog`
- [ ] `HealingActionType`, `HealingStatus` enum

### 2.2 Healing Actions
- [ ] `RestartService` — `systemctl restart {service}` via SSH
- [ ] `ClearCache` — Redis FLUSHDB + reload PHP-FPM (clear OPCache)
- [ ] `RunCommand` — custom whitelisted command
- [ ] `ScaleWorker` — PM2 scale / restart queue workers

### 2.3 Healing Engine
- [ ] `HealingRuleEngine` — triggered by Alert
- [ ] Check: rule enabled? cooldown passed? max_attempts not exceeded?
- [ ] Execute action → log result → update alert jika sukses
- [ ] `ExecuteHealingJob`
- [ ] `HealingLogger`

### 2.4 Self Healing UI
- [ ] `SelfHealingController@indexRules`, `@storeRule`, `@showRule`, `@updateRule`, `@destroyRule`, `@toggleRule`, `@executeRule`
- [ ] `SelfHealingController@indexLogs`, `@showLog`
- [ ] `HealingRuleResource`, `HealingLogResource`
- [ ] UI: healing rule list + form (pilih alert rule trigger, action, params)
- [ ] UI: healing log timeline, grouped by server

### 2.5 Scheduler Update
- [ ] `DispatchAlertJob` dipanggil dari `EvaluateAlertRules` listener
- [ ] `ExecuteHealingJob` dipanggil dari `EvaluateAlertRules` listener jika ada healing rule

---

## Acceptance Criteria v1.2

- [ ] Bisa tambah/kelola channel Telegram, Discord, Email
- [ ] Test channel berfungsi (kirim test alert)
- [ ] Alert terkirim otomatis ke semua channel enabled saat rule terpicu
- [ ] Alert bisa di-acknowledge dan di-resolve dari dashboard
- [ ] Healing rule bisa dibuat: "jika Nginx down → restart Nginx"
- [ ] Healing auto-eksekusi saat alert trigger (jika rule enabled)
- [ ] Healing cooldown + max_attempts dihormati
- [ ] Healing log mencatat semua eksekusi + hasil
- [ ] Healing bisa dieksekusi manual dari UI
- [ ] Semua test pass

---

## File Checklist v1.2

```
Models (4)     : AlertChannel, AlertNotification, HealingRule, HealingLog
Controllers (3): AlertChannelController, AlertNotificationController, SelfHealingController
Services (10)  : AlertDispatcher, TelegramChannel, DiscordChannel, EmailChannel,
                 HealingRuleEngine, RestartService, ClearCache, RunCommand, ScaleWorker, HealingLogger
Jobs (2)       : DispatchAlertJob, ExecuteHealingJob
Events (2)     : AlertTriggered, HealingExecuted
Listeners (2)  : SendAlertNotification, (update EvaluateAlertRules)
Enums (3)      : AlertChannelType, HealingActionType, HealingStatus
Migrations (4) : alert_channels, alert_notifications, healing_rules, healing_logs
Tests (4)      : AlertDispatcherTest, TelegramChannelTest, HealingRuleEngineTest, SelfHealingControllerTest
```
